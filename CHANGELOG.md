# Project Changelog

All notable changes to the SignBridge project will be documented in this file.

The format is based on Keep a Changelog.

## [Unreleased]

### Added

- Created this root-level CHANGELOG.md to track overall project progress.
- Installed frontend and backend dependencies.
- Successfully verified startup commands for both backend and frontend servers.
- Installed `ffmpeg` globally into `C:\ffmpeg\bin` and explicitly configured the backend to use this path to permanently resolve audio extraction errors.
- Successfully installed `Ollama` and downloaded the `mistral` model to power the backend translation engine.
